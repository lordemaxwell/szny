# macOS
.DS_Store
**/.DS_Store

# dependencies
node_modules/
.superpowers/
_site/
output/
.playwright-cli/

# personal resume (single-page html, kept out of the website)
resume/

# vercel local env + secrets (KV tokens) — never commit
.vercel/
.env*.local
